options:
  continue_on_error: false
  report_format: gotest
  jobs: 2
tests:
  - name: one
    suites:
      - "all"
    type: shell-operation-error
    args:
      command: "emacs --version"
  - name: two-or-what
    type: shell-operation
    suites:
      - "all"
    args:
      command: "foo --version"
